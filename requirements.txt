# ============================================
# CADRE — Continual Adaptation for Driving
# with Robust Evolution
# ============================================
# NOTE: Install PyTorch FIRST (with CUDA) before
# running pip install -r requirements.txt
# See README.md Step 3 for PyTorch install commands.
# ============================================

# ── Vision-Language Model ──
transformers>=4.40.0
accelerate>=0.30.0
huggingface_hub>=0.23.0

# ── Parameter-Efficient Fine-Tuning ──
peft>=0.11.0

# ── Dataset Tools ──
nuscenes-devkit>=1.1.11
kaggle>=1.6.0

# ── Scientific Computing ──
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0
pandas>=2.0.0

# ── Configuration ──
pyyaml>=6.0
omegaconf>=2.3.0

# ── Logging & Visualization ──
tensorboard>=2.14.0
matplotlib>=3.7.0
seaborn>=0.12.0
tqdm>=4.65.0
rich>=13.0.0

# ── Image Processing ──
Pillow>=10.0.0
opencv-python>=4.8.0

# ── Evaluation ──
pycocotools>=2.0.7

# ── Utilities ──
einops>=0.7.0
safetensors>=0.4.0
